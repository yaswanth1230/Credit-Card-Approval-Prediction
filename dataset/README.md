# Dataset

Place the credit card dataset CSV files in this folder when running analysis locally.

Large CSV files are ignored by Git using `.gitignore` because GitHub blocks normal uploads above 100 MB. Keep the dataset locally or upload it using Git LFS if required.

